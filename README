strapi info:
localhost:1337

username: test@admin.com
password: test132

code for searchbar (it works, but having major issues when deploying it to netlify):

// import React, { useRef } from "react";

// export const liveSearch = (event: any) => {
// let cards = document.querySelectorAll('.flip-card');
// console.log(event)
// let search_query : string = (document.getElementById("searchbox") as HTMLInputElement).value;
// for (let i = 0; i < cards.length; i++) {
// if (cards[i].innerText.toLowerCase().includes(search_query.toLowerCase())) {
// cards[i].classList.remove('hidden');
// } else {
// cards[i].classList.add('hidden');
// }
// }
// }
